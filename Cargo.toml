[package]
name = "wriggle-tune"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.32", features = ["derive"] }
ctrlc = "3.4.5"
midir = { version = "0.10.0" }
rand = "0.9.0"

[features]
jack_audio = ["midir/jack"]